# Keep this congruent with `.github/workflows/ci.yml`.

# The clang shipped on macOS doesn't support BPF, so we need LLVM from brew.
brew "llvm"

# protoc compiles the controller's gRPC `.proto` (velstra-proto build script).
brew "protobuf"
