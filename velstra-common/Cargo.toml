[package]
name = "velstra-common"
version = "0.1.0"
edition.workspace = true

license = "MIT OR Apache-2.0"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { workspace = true, optional = true }

[lib]
path = "src/lib.rs"
